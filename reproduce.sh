#!/usr/bin/env bash
# reproduce.sh — Single-command pipeline reproduction
#
# Conforms to PUBLIC_MIRROR_STANDARD.md v1.0.0.
#
# This repository is a documentation + template + prompt toolkit. It ships no
# Python package, no computational pipeline, and no derived numerical
# artifacts. There is nothing to reproduce from inputs in the
# I4Replication/AI-Games sense. This script exists to satisfy the standard's
# orchestrator slot and to signal that fact explicitly to humans + audit tools.
#
# For paper computational pipelines see the per-paper mirrors under
# https://github.com/spectralbranding/orgschema-papers.

set -euo pipefail

echo "OST toolkit — docs and templates; nothing to reproduce. See https://github.com/spectralbranding/orgschema-papers for paper mirrors."
exit 0
